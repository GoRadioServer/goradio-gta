# Copy to secret.yaml, fill in the real JWT (the one that was in
# station.yaml before it was stripped out -- see ../station.yaml's auth
# comment for how to mint another via `radio tokengen`), and apply that
# instead. secret.yaml is gitignored; this template is not.
apiVersion: v1
kind: Secret
metadata:
  name: goradio-gtasa-secrets
type: Opaque
stringData:
  jwt: "CHANGE_ME"
