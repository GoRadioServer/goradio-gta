# Copy to secret.yaml, fill in a real JWT -- mint one with
# ../scripts/mint-token.sh (needs the audio server's own auth.jwt_secret;
# see that script's header) -- and apply that instead. secret.yaml is
# gitignored; this template is not.
apiVersion: v1
kind: Secret
metadata:
  name: goradio-gtasa-secrets
type: Opaque
stringData:
  jwt: "CHANGE_ME"
