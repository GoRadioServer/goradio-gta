# Only station.lua, station.yaml, ecosystem.config.js, and
# data/gtasa-stations.json are actually COPYed into the image -- the rest
# of this directory (918MB of audio, the local `radio` binary already in
# the base image, transcode cache, lua-types) would otherwise bloat every
# build context for nothing.
data/audio/
data/transcode-cache/
radio
lua-types/
*.example.yaml
server.yaml
GETTING_STARTED.txt
.luarc.json
